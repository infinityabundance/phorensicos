// The Phase 7 autonomous-campaign revision 0: a deliberately wrong port that a
// candidate producer might propose first. It returns 0 for every input, so the
// design court rejects it immediately and the CEGIS loop revises.
//
// It is committed so the autonomous seal's closure (which binds the rejected
// revision's FRF receipt) reproduces byte-for-byte from a clean checkout.
package "phorensic:jit_port_strspn_always_zero:v1.0";

fn phor_strspn_len(ws: u64, wa: u64, n: u64) -> u64 effect [compute] { return 0; }
