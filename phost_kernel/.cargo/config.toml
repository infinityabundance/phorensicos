[build]
target = "x86_64-unknown-none"

# Non-PIE, fixed at 1 MiB via linker.ld, entry = _start (the 32-bit stub).
# The stub object, linker script, and entry are passed from build_kernel.sh
# with absolute paths.
[target.x86_64-unknown-none]
rustflags = [
    "-C", "link-arg=-no-pie",
]
