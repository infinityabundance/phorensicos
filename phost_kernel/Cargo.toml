[package]
name = "phost_kernel"
version = "0.1.0"
edition = "2021"

[lib]
name = "phost_kernel"
crate-type = ["staticlib"]

[dependencies]
phost = { path = "../phost", default-features = false, features = ["alloc"] }
